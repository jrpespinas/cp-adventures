#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int itr, line=2;
	cin >> itr;

	for(int i=0; i<itr; i++)
		line += pow(2, i);
	cout << line * line;

	return 0;
}
